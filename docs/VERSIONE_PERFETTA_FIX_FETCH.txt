CivicVois - Versione PERFETTA fix fetch

Questa versione rimuove completamente la dipendenza da supabase-js via CDN.
Usa fetch diretto verso:
- /auth/v1 per registrazione/login
- /rest/v1 per tabelle
- /storage/v1 per immagini

La publishable key viene inviata SOLO come header apikey.
Il Bearer viene usato SOLO quando esiste il vero token JWT dell'utente dopo login.

Non serve rieseguire schema.sql se è già stato eseguito con successo.
Carica lo ZIP su Netlify e testa prima dal dominio keen-palmier-430d1a.netlify.app,
poi da civicvois.it quando HTTPS è attivo.
